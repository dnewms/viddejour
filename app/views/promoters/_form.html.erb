<%= form_for(@promoter) do |f| %>
  <% if @promoter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promoter.errors.count, "error") %> prohibited this promoter from being saved:</h2>

      <ul>
      <% @promoter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
